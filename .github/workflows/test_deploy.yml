name: Lint, Test, Deploy
on: push
jobs:
  clone:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout
  lint:
    needs: clone
    runs-on: ubuntu-latest
    steps:
      - name: Run lint
        run: npm run lint
  test:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: npm run test
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Build project
        run: npm run build
